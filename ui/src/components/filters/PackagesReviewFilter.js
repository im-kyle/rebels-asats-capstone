import React from 'react';
import {
  Typography,
} from '@mui/material';

function PackagesReviewFilter() {

  return (
    <Typography variant='h6'>
      Packages For Review Filter
    </Typography>
  )
}

export default PackagesReviewFilter;